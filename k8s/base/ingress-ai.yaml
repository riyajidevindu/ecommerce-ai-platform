apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ai-ingress
  namespace: ecommerce
  annotations:
    kubernetes.io/ingress.class: traefik
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
    traefik.ingress.kubernetes.io/router.tls: "true"
    traefik.ingress.kubernetes.io/router.middlewares: ecommerce-ai-strip@kubernetescrd
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - ai.ecomaiselleragent.store
      secretName: ai-ecomaiselleragent-store-tls
  rules:
    - host: ai.ecomaiselleragent.store
      http:
        paths:
          - path: /api/v1/ai
            pathType: Prefix
            backend:
              service:
                name: ai-orchestrator-service
                port:
                  number: 8003
