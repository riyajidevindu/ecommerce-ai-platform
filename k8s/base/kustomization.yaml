apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: ecommerce

resources:
  - ns.yaml
  - cluster-issuer.yaml
  - certificate.yaml
  - traefik-middleware-redirect.yaml
  - traefik-ingress.yaml
  - traefik-ingress-http.yaml
  # Use a single ingress with full prefixed paths; disable per-service ingresses and strip middlewares to avoid route conflicts
  - postgres-headless-alias.yaml
  - rabbitmq-secret.yaml
  - rabbitmq.yaml
  - auth-service.yaml
  - stock-service.yaml
  - whatsapp-connector-service.yaml
  - ai-orchestrator-service.yaml
  - notification-service.yaml
  - file-storage-service.yaml


