apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: whatsapp-ingress
  namespace: ecommerce
  annotations:
    kubernetes.io/ingress.class: traefik
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
    traefik.ingress.kubernetes.io/router.tls: "true"
    traefik.ingress.kubernetes.io/router.middlewares: ecommerce-whatsapp-strip@kubernetescrd
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - whatsapp.ecomaiselleragent.store
      secretName: whatsapp-ecomaiselleragent-store-tls
  rules:
    - host: whatsapp.ecomaiselleragent.store
      http:
        paths:
          - path: /api/v1/whatsapp
            pathType: Prefix
            backend:
              service:
                name: whatsapp-connector-service
                port:
                  number: 8002
